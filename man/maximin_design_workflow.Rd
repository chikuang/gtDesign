% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/maximin_design_workflow.R
\name{maximin_design_workflow}
\alias{maximin_design_workflow}
\title{Maximin design with reference losses, equivalence, and eta (workflow)}
\usage{
maximin_design_workflow(
  u,
  f,
  criteria,
  opts = list(),
  drop_tol = 1e-06,
  q = NULL,
  tol_eta = 0.001,
  tol_equiv = 0.002,
  check_equiv = TRUE,
  keep_reference_designs = FALSE,
  info_weight = NULL,
  solver = "CLARABEL",
  eta_args = list(),
  deriv_args = list(),
  make_figure = FALSE,
  plot_args = list(),
  ...
)
}
\arguments{
\item{u}{Candidate design points.}

\item{f}{Regression function (same as in single-objective and maximin calls).}

\item{criteria}{Character vector of criteria, subset of \code{c("D", "A", "c")}.}

\item{opts}{Named list passed to contrast-based criteria; use \code{cVec_c} for \strong{c}.}

\item{drop_tol}{Passed to \code{\link[=calc_Dopt]{calc_Dopt()}}, \code{\link[=calc_Aopt]{calc_Aopt()}}, \code{\link[=calc_copt]{calc_copt()}}.}

\item{q}{Parameter dimension for \code{\link[=calc_eta_weights_maximin]{calc_eta_weights_maximin()}}; default is the
length of \code{f(u[1])}.}

\item{tol_eta}{Tolerance passed to \code{\link[=calc_eta_weights_maximin]{calc_eta_weights_maximin()}} (unless
overridden in \code{eta_args}).}

\item{tol_equiv}{Tolerance for \code{\link[=check_equivalence_maximin]{check_equivalence_maximin()}} when
\code{check_equiv} is \code{TRUE}.}

\item{check_equiv}{If \code{TRUE}, run \code{\link[=check_equivalence_maximin]{check_equivalence_maximin()}}.}

\item{keep_reference_designs}{If \code{TRUE}, include full outputs of each
single-objective solve in \code{reference_designs}.}

\item{info_weight}{Passed to \code{\link[=compute_maximin_design]{compute_maximin_design()}}.}

\item{solver}{Passed to all convex solves in this pipeline.}

\item{eta_args}{Named list of extra arguments for \code{\link[=calc_eta_weights_maximin]{calc_eta_weights_maximin()}}
(e.g. \code{list(solver = "SCS", complementary_slack = FALSE)}).}

\item{deriv_args}{Named list of extra arguments for \code{\link[=calc_directional_derivatives]{calc_directional_derivatives()}}.}

\item{make_figure}{If \code{TRUE}, draw the maximin equivalence figure via
\code{\link[=plot_equivalence_maximin]{plot_equivalence_maximin()}} (opens a graphics device).}

\item{plot_args}{Named list of extra arguments for \code{\link[=plot_equivalence_maximin]{plot_equivalence_maximin()}}
(merged over defaults that pass \code{tol = tol_equiv}).}

\item{...}{Additional arguments passed to \code{\link[=compute_maximin_design]{compute_maximin_design()}} (e.g.
\code{support_tol}).}
}
\value{
A list with components: \code{loss_ref}, \code{maximin} (output of
\code{\link[=compute_maximin_design]{compute_maximin_design()}}), \code{directional_derivatives}, \code{eta},
\code{equivalence} (or \code{NULL} if \code{check_equiv} is \code{FALSE}), \code{q}, and optionally
\code{reference_designs}. Plotting is a side effect when \code{make_figure} is \code{TRUE}.
}
\description{
Runs the usual multi-step maximin pipeline: single-objective designs to build
\code{loss_ref}, \code{\link[=compute_maximin_design]{compute_maximin_design()}}, \code{\link[=calc_directional_derivatives]{calc_directional_derivatives()}},
\code{\link[=calc_eta_weights_maximin]{calc_eta_weights_maximin()}}, and optionally \code{\link[=check_equivalence_maximin]{check_equivalence_maximin()}}.
}
\details{
Only criteria \strong{D}, \strong{A}, and \strong{c} are supported end-to-end (directional
derivatives for \strong{Ds} / \strong{E} are not wired here). For \strong{c}, supply
\code{opts = list(cVec_c = ...)} as in \code{\link[=compute_maximin_design]{compute_maximin_design()}}.
}
