% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/util.R
\name{exact_design_efficiency_maximin}
\alias{exact_design_efficiency_maximin}
\title{Efficiencies of an exact design relative to single-objective optima (maximin)}
\usage{
exact_design_efficiency_maximin(
  M_exact,
  loss_ref,
  criteria,
  opts = list(),
  p = nrow(M_exact)
)
}
\arguments{
\item{M_exact}{Information matrix of the exact (rounded) design (e.g.
\code{out$M_exact} from \code{\link[=round_gt_design_budget]{round_gt_design_budget()}}).}

\item{loss_ref}{Named list of reference losses from single-objective designs,
on the same scale as \code{\link[=compute_maximin_design]{compute_maximin_design()}}.}

\item{criteria}{Character vector of criteria (e.g. \code{c("D", "A")}).}

\item{opts}{Optional list for contrasts (\code{cVec_Ds}, \code{cVec_c}) for
\eqn{D_s}- and \eqn{c}-type criteria.}

\item{p}{Parameter dimension; defaults to \code{nrow(M_exact)}.}
}
\value{
A list with \code{efficiencies} (named numeric vector) and
\code{min_efficiency} (\eqn{\min_j \mathrm{Eff}_j}).
}
\description{
Computes criterion-wise efficiencies as in eq. (5) of Yeh, Wong, and Zhou
(arXiv:2508.08445), and their minimum (\strong{MinEff} in Table 4). Use after
\code{\link[=round_gt_design_budget]{round_gt_design_budget()}} with a maximin approximate design: pass
\code{M_exact = out$M_exact}, the same \code{loss_ref} as in \code{\link[=compute_maximin_design]{compute_maximin_design()}},
and the same \code{criteria} vector.
}
