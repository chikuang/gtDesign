% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rounding_maximin_exact.R
\name{round_gt_design_n_maximin}
\alias{round_gt_design_n_maximin}
\title{Exact maximin design under fixed run size (Algorithm I + search)}
\usage{
round_gt_design_n_maximin(
  approx_design,
  u,
  theta,
  n,
  q_cost,
  loss_ref,
  criteria,
  opts = list(),
  n_index = 2L,
  ...
)
}
\arguments{
\item{approx_design}{A maximin/approximate design object with component
\code{design} containing columns \code{point} and \code{weight} (e.g. output of
\code{\link[=compute_maximin_design]{compute_maximin_design()}}).}

\item{u}{Integer candidate pool sizes (same grid as used for
\code{approx_design}).}

\item{theta}{Nominal \eqn{(p_0,p_1,p_2)}.}

\item{n}{Total run size (fixed sample size).}

\item{q_cost}{Cost ratio \eqn{q} in \eqn{c(x)=1-q+qx}.}

\item{loss_ref}{Named list of reference losses from single-objective designs
(same scale as \code{\link[=compute_maximin_design]{compute_maximin_design()}}).}

\item{criteria}{Character vector of criteria to evaluate MinEff, e.g.
\code{c("D","A")} or \code{c("D","A","Ds")}.}

\item{opts}{Contrast options: \code{cVec_c} and/or \code{cVec_Ds} when needed.}

\item{n_index}{Half-width of extension window around each support point.}

\item{...}{Reserved.}
}
\value{
A list with \code{design_round1}, \code{design_exact}, \code{delta},
\code{M_round1}, \code{M_exact}, \code{efficiencies} (named vector), \code{min_efficiency},
and \code{extension_table} (tibble sorted by decreasing \code{min_efficiency}).
}
\description{
Rounds a maximin approximate design to an exact design with total run size
\code{n}, then applies the modified Step II search over nearby support points
(\eqn{x_i \pm} \code{n_index}) and selects the extension with the largest
minimum efficiency (MinEff) across \code{criteria}.
}
