% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gt_huang2020.R
\name{gt_huang2020_regressor}
\alias{gt_huang2020_regressor}
\title{Effective regressor \eqn{\sqrt{\lambda(x)}\,\mathbf{f}(x)} for convex design code}
\usage{
gt_huang2020_regressor(theta, q = 0)
}
\arguments{
\item{theta}{Nominal \eqn{(p_0,p_1,p_2)} for local optimality.}

\item{q}{Cost ratio in \verb{[0, 1]}; use \code{0} for constant cost per test.}
}
\value{
A function \verb{function(x)} returning a length-3 vector.
}
\description{
The information matrix in Huang et al. (2020) is
\eqn{\sum_i w_i \lambda(x_i) \mathbf{f}(x_i)\mathbf{f}(x_i)^\top}.
The same matrix equals \eqn{\sum_i w_i \mathbf{h}(x_i)\mathbf{h}(x_i)^\top} with
\eqn{\mathbf{h}(x)=\sqrt{\lambda(x)}\mathbf{f}(x)}, which matches the
regression form used by \code{\link[=calc_Dopt]{calc_Dopt()}} and \code{\link[=check_equivalence]{check_equivalence()}} without an
\code{info_weight} argument.
}
\references{
Yeh, C.-K., Wong, W. K., and Zhou, J. (2025). Single and multi-objective
optimal designs for group testing experiments. arXiv:2508.08445.

Huang, S.-Y., Chen, Y.-H., and Wang, W. (2020). Optimal group testing designs
for estimating prevalence with imperfect tests. Journal of the Royal
Statistical Society Series C.
}
