% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calc_Eopt.R
\name{calc_Eopt}
\alias{calc_Eopt}
\title{E-optimal approximate design for group testing experiments}
\usage{
calc_Eopt(u, f, solver = "CLARABEL", ..., drop_tol = 1e-08, ridge = 1e-08)
}
\arguments{
\item{u}{Candidate design points (e.g. pool sizes or coded pool layouts).}

\item{f}{Function returning a numeric vector at one design point.}

\item{solver}{Solver name passed to CVXR. Default is \code{"CLARABEL"}.}

\item{...}{Additional arguments passed to \code{\link[CVXR:psolve]{CVXR::psolve()}}.}

\item{drop_tol}{Threshold for removing near-zero weights.}

\item{ridge}{Small diagonal ridge for numerical stability in \code{log_det}.}
}
\value{
An object of class \code{"gt_e_design"} and \code{"gt_design"}. Components
\code{value} and \code{optval} are the minimization objective
\eqn{-\lambda_{\min}(\mathbf{M})}: \code{value} is recomputed at the cleaned
support, \code{optval} is the solver optimum of the minimize objective (they may
differ slightly after \code{drop_tol}).
}
\description{
Minimizes \eqn{-\lambda_{\min}(\mathbf{M})} over \eqn{\mathbf{w}} on a finite
candidate set via \code{CVXR::Minimize(-CVXR::lambda_min(\ldots))}. The
reported objective is \eqn{\mathrm{loss}(\mathbf{M}) = -\lambda_{\min}(\mathbf{M})},
matching MATLAB \code{loss = -lambda_min(FIM)} (e.g.\ \code{calc_loss_E}).
}
\details{
This matches \code{\link[=compute_design_SO]{compute_design_SO()}} with \code{criterion = "E"} (same scalar loss).
}
\examples{
\donttest{
# Huang et al. (2020) model on {1,...,61}, q = 0 (same setting as D-opt example)
theta <- c(p0 = 0.07, p1 = 0.93, p2 = 0.96)
u <- seq_len(61L)
f <- gt_huang2020_regressor(theta, q = 0)
res_E <- calc_Eopt(u, f, drop_tol = 1e-6)
res_E$design
res_E$value # = -lambda_min(M), same as -min(eigen(M)$values)
-res_E$value # lambda_min(M) at the optimum
res_E$status
}

}
