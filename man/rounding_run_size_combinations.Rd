% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rounding_combinations.R
\name{rounding_run_size_combinations}
\alias{rounding_run_size_combinations}
\title{Enumerate nonnegative integer allocations (run-size rounding)}
\usage{
rounding_run_size_combinations(n_items, max_run_size)
}
\arguments{
\item{n_items}{Number of locations (columns).}

\item{max_run_size}{Upper bound on \eqn{\sum_j z_j}.}
}
\value{
Numeric matrix with columns \eqn{z_1,\ldots,z_m}, then total count,
then remaining slots (\verb{max_run_size -} total), sorted lexicographically
with the last coordinate changing fastest (MATLAB \code{sortrows} order).
}
\description{
All vectors \eqn{(z_1,\ldots,z_m)} with \eqn{z_j \ge 0}, \eqn{\sum_j z_j \le}
\code{max_run_size}, matching the supplementary \strong{Rounding Algorithm I} search
(arXiv:2508.08445, Sec. 5.1).
}
