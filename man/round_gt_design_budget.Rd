% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rounding_gt_budget.R
\name{round_gt_design_budget}
\alias{round_gt_design_budget}
\title{Exact group-testing design under a fixed budget (Rounding Algorithm II)}
\usage{
round_gt_design_budget(
  approx_design,
  u,
  theta,
  C,
  q_cost,
  criterion = c("D", "A", "c", "Ds", "E"),
  opts = list(),
  n_index = 2L,
  fix_zero_floor = TRUE,
  repair_floor_budget = TRUE,
  ...
)
}
\arguments{
\item{approx_design}{Output of \code{\link[=calc_Dopt]{calc_Dopt()}}, \code{\link[=calc_Aopt]{calc_Aopt()}}, \code{\link[=calc_copt]{calc_copt()}},
\code{\link[=calc_Eopt]{calc_Eopt()}}, or \code{\link[=compute_design_SO]{compute_design_SO()}} on the same candidate set \code{u}.}

\item{u}{Integer candidate pool sizes (same grid as used for \code{approx_design}).}

\item{theta}{Nominal \eqn{(p_0,p_1,p_2)}.}

\item{C}{Total cost budget (sum of run costs \eqn{n_i c(x_i)}).}

\item{q_cost}{Cost ratio \eqn{q} in \eqn{c(x)=1-q+qx}.}

\item{criterion}{One of \code{"D"}, \code{"A"}, \code{"c"}, \code{"Ds"}, \code{"E"}.}

\item{opts}{Contrast options: \code{cVec_c} and/or \code{cVec_Ds} when needed (not used for \code{"E"}).}

\item{n_index}{Half-width of the extended support window (\eqn{x_i\pm} \code{n_index}).}

\item{fix_zero_floor}{If \code{TRUE} (default), redistribute runs so no support
pool has zero runs after \code{floor}, as in \code{GT_MO_rounding_budget.m}. If
\code{FALSE}, skip this step (as in \code{GT_Single_budget.m}, which only floors).}

\item{repair_floor_budget}{If \code{TRUE} (default), after zero-fix, remove runs
from the most expensive active pools until \eqn{\sum_i n_i c(x_i) \le C},
so \code{design_round1} never exceeds the budget. Zero-fix can increase total
cost because \eqn{c(x)} varies by pool size; without this step the floor
design can strictly overspend. Set to \code{FALSE} only to match reference
MATLAB MO scripts that skip this repair (then overspend is possible).}

\item{...}{Reserved.}
}
\value{
A list with \code{design_round1} (2-row matrix: pool sizes, floor counts;
columns with zero runs are removed; no column names),
\code{design_exact} (same format after merge),
\code{delta} (2-row matrix: pool sizes \code{x} and \code{Delta_n} \eqn{= n_{\mathrm{exact}}
  - n_{\mathrm{round1}}}; only pool sizes with nonzero change are columns),
\code{M_approx},
\code{M_exact}, \code{efficiency}, \code{loss_approx}, \code{loss_exact}, \code{C_remaining} after
the floor step (with default \code{repair_floor_budget}, total cost is at most
\code{C}): \eqn{\max\{0, \mathrm{round}(C - \sum_i n_i c(x_i), 4)\}}
(never negative), \code{extension_table} (\code{\link[tibble:tibble]{tibble::tibble()}} of tight extensions;
first columns named by extended-support pool sizes, then
\code{used_cost}, \code{remaining}, \code{loss} when present), and
\code{criterion}.
}
\description{
Implements the floor allocation, zero-fix, extended-support search, and
best-merge steps in Sec. 5.1 of Yeh, Wong, and Zhou (arXiv:2508.08445), as
in the reference MATLAB scripts \code{GT_Single_budget.m} /
\code{calc_budget_round_combinations.m}.
}
