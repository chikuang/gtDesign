% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compute_design_SO.R
\name{compute_design_SO}
\alias{compute_design_SO}
\title{Single-objective optimal approximate design (group testing)}
\usage{
compute_design_SO(
  u,
  f,
  criterion = c("D", "A", "Ds", "c", "E"),
  opts = list(),
  info_weight = NULL,
  solver = "CLARABEL",
  ...,
  support_tol = 1e-04,
  drop_tol = 1e-08
)
}
\arguments{
\item{u}{Candidate design points.}

\item{f}{A function returning the regression vector at a single design point.}

\item{criterion}{One of \code{"D"}, \code{"A"}, \code{"Ds"}, \code{"c"}, or \code{"E"}.}

\item{opts}{Named list of extra options. For \code{"Ds"} use \code{opts$cVec_Ds}; for
\code{"c"} use \code{opts$cVec_c}.}

\item{info_weight}{Optional function returning a nonnegative scalar multiplier
for each rank-one information contribution.}

\item{solver}{Solver passed to \code{\link[CVXR:psolve]{CVXR::psolve()}}.}

\item{...}{Additional arguments passed to \code{\link[CVXR:psolve]{CVXR::psolve()}}.}

\item{support_tol}{Weights smaller than this are dropped from the reported
support.}

\item{drop_tol}{Numerical tolerance for tiny solver noise before support
cleanup.}
}
\value{
A list of class \code{"gt_so_design"} and \code{"gt_design"}.
}
\description{
Solves a convex optimization problem over a finite candidate set for one
criterion. The regression function \code{f} encodes rank-one contributions to the
(approximate) information matrix, as in nonlinear design or group testing
Fisher information assembled from pool outcomes.
}
