% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rounding_maximin_exact.R
\name{round_gt_design_budget_maximin}
\alias{round_gt_design_budget_maximin}
\title{Exact maximin design under fixed budget (Algorithm II + search)}
\usage{
round_gt_design_budget_maximin(
  approx_design,
  u,
  theta,
  C,
  q_cost,
  loss_ref,
  criteria,
  opts = list(),
  n_index = 2L,
  fix_zero_floor = TRUE,
  repair_floor_budget = TRUE,
  ...
)
}
\arguments{
\item{approx_design}{A maximin/approximate design object with component
\code{design} containing columns \code{point} and \code{weight} (e.g. output of
\code{\link[=compute_maximin_design]{compute_maximin_design()}}).}

\item{u}{Integer candidate pool sizes (same grid as used for
\code{approx_design}).}

\item{theta}{Nominal \eqn{(p_0,p_1,p_2)}.}

\item{C}{Total cost budget (sum of run costs \eqn{n_i c(x_i)}).}

\item{q_cost}{Cost ratio \eqn{q} in \eqn{c(x)=1-q+qx}.}

\item{loss_ref}{Named list of reference losses from single-objective designs
(same scale as \code{\link[=compute_maximin_design]{compute_maximin_design()}}).}

\item{criteria}{Character vector of criteria to evaluate MinEff, e.g.
\code{c("D","A")} or \code{c("D","A","Ds")}.}

\item{opts}{Contrast options: \code{cVec_c} and/or \code{cVec_Ds} when needed.}

\item{n_index}{Half-width of extension window around each support point.}

\item{fix_zero_floor}{If \code{TRUE} (default), run MATLAB-style zero-fix after
floor allocation.}

\item{repair_floor_budget}{If \code{TRUE} (default), cap floor design to satisfy
budget by removing runs from the most expensive active pools.}

\item{...}{Reserved.}
}
\value{
A list with \code{design_round1}, \code{design_exact}, \code{delta}, \code{M_round1},
\code{M_exact}, \code{efficiencies}, \code{min_efficiency}, \code{C_remaining} after floor,
and \code{extension_table} (tibble sorted by decreasing \code{min_efficiency}).
}
\description{
Applies cost-aware floor rounding, then modified Step II search over nearby
support points (\eqn{x_i \pm} \code{n_index}) and selects the extension with the
largest minimum efficiency (MinEff) across \code{criteria}.
}
