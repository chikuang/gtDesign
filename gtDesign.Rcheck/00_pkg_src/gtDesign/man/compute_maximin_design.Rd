% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compute_maximin_design.R
\name{compute_maximin_design}
\alias{compute_maximin_design}
\title{Maximin multi-criterion approximate design (group testing)}
\usage{
compute_maximin_design(
  u,
  f,
  loss_ref,
  criteria,
  opts = list(),
  info_weight = NULL,
  solver = "CLARABEL",
  ...,
  support_tol = 1e-04,
  drop_tol = 1e-08
)
}
\arguments{
\item{u}{Candidate design points.}

\item{f}{A function returning the regression vector at a single design point.}

\item{loss_ref}{Named list of reference losses from single-objective
designs, on the same scale as \code{\link[=compute_design_SO]{compute_design_SO()}} internal losses.}

\item{criteria}{Character vector containing any of \code{"D"}, \code{"A"}, \code{"Ds"},
\code{"c"}, or \code{"E"}.}

\item{opts}{Named list of extra options. For \code{"Ds"} use \code{opts$cVec_Ds}; for
\code{"c"} use \code{opts$cVec_c}.}

\item{info_weight}{Optional function returning a nonnegative scalar multiplier
for each rank-one information contribution.}

\item{solver}{Solver passed to \code{\link[CVXR:psolve]{CVXR::psolve()}}.}

\item{...}{Additional arguments passed to \code{\link[CVXR:psolve]{CVXR::psolve()}}.}

\item{support_tol}{Weights smaller than this are dropped from the reported
support.}

\item{drop_tol}{Numerical tolerance for tiny solver noise before support
cleanup.}
}
\value{
A list of class \code{"gt_maximin_design"} and \code{"gt_design"}.
}
\description{
Joint convex formulation for maximizing the minimum efficiency across several
criteria, using reference losses from single-objective designs. Suitable
when multiple experimental goals (e.g. overall prevalence versus contrasts)
must be balanced in group testing allocation.
}
